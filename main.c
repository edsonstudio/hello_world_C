#include <stdio.h>  //stdio = Standard Input Output
#include <conio.h>  //importação necessaria para utilizar o getch();

int main(){     //void ou int

    printf("Hello World!!!");  //printf = imprimir de forma formatada

    getch();                   //getch() equivalente ao Console.ReadKey() do C#, 
                                // que aguarda pressionar qualquer tecla para finalizar a execução.

    return 0;

}